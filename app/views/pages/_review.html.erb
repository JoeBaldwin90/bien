<p>
  <%= link_to review.restaurant, review_path(review) %> (<%= review.score %>/10)
  <% if review.user.present? %>
    <p class="small">Rated by <%= link_to review.user.username, user_path(review.user.username) %></p>
  <% else %>
    Anon)
  <% end %>
</p>
